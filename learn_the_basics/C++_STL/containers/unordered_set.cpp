#include<bits/stdc++.h>
using namespace std;
void explainUset(){
    //stores unique element but in unordered (i.e,non sorted) way
    unordered_set<int> u;
    //lower_bound and upper bound function doesnt work
    //rest functions are same as set
    //O(1) 99.9% time else O(n)
    }